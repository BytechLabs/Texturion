<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Calls (#155): softphone audio + self-managed telecom integration. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Notifications (#156): Android 13+ runtime permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".LoonextApp"
        android:label="Loonext"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loonext.Starting">

        <!-- singleTask: the browser's OAuth redirect must return to the RUNNING
             instance via onNewIntent, not stack a new one in the browser task. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Google sign-in PKCE redirect (#166). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.loonext.android" android:host="auth-callback" />
            </intent-filter>
        </activity>

        <!-- #171 bugs 3+4: the dedicated full-screen incoming-call surface, the
             fullScreenIntent target of the CallStyle ring (NEVER MainActivity's
             tab shell). showWhenLocked + turnScreenOn render answer/decline over
             the keyguard; the code additionally calls setShowWhenLocked/
             setTurnScreenOn (API 27+) and requestDismissKeyguard on answer.
             excludeFromRecents + its own empty taskAffinity keep the lock-screen
             ring out of the app's back stack; singleInstance so a push-immediate
             launch and a later notification tap reuse the one presentation.
             Not exported — only our notification PendingIntents launch it. -->
        <activity
            android:name=".features.calls.IncomingCallActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Loonext" />

        <!-- FCM entry point (#156): message + token-refresh delivery. -->
        <service
            android:name=".push.LoonextMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Self-managed telecom integration (#155). exported=true gated by the
             BIND_TELECOM_CONNECTION_SERVICE permission is the framework contract. -->
        <service
            android:name=".telephony.LoonextConnectionService"
            android:exported="true"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>
        <receiver
            android:name=".telephony.CallActionReceiver"
            android:exported="false" />
    </application>
</manifest>
