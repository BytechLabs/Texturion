package com.loonext.android.ui.theme

import androidx.compose.material3.Typography

/**
 * Default Material typography for now; the Golos Text bundle lands with the
 * design-polish pass so every screen flips at once.
 */
val LoonextTypography = Typography()
