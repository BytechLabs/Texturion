/**
 * Marketing-scoped CSS primitive: the small drawn marks the surface still uses.
 *
 * This was `marketing/ledger/` exporting `LedgerStyles`, and the name outlived
 * what it named. The ledger COSTUME is gone (DESIGN-DIRECTION §0): no 01…12
 * spine numbering, no section-number eyebrow, no status-spine / FILED ticket, no
 * self-drawing "done" glyph from the old grammar. A directory called `ledger/`
 * was the last place someone grepping for the superseded identity would find a
 * live hit, which is the #372 failure in code form rather than in a doc.
 */

export { MarketingMarks } from "./marks-css";
