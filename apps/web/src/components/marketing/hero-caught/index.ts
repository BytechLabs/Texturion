/**
 * The signature "Caught" hero (DESIGN-DIRECTION §2, §3). The one place the site
 * spends its boldness: the customer text that would have been missed, now caught
 * and claimed by a name. Replaces the superseded Dispatch Desk hero.
 */

export { CaughtHero } from "./caught-hero";
