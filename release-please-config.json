{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",

  "//": [
    "ONE product version for everything. Loonext 1.3.0 means the same thing in",
    "the web app, the API, and both store listings — because to a customer it IS",
    "one product. A package-per-app layout would make 'what version are we on?' a",
    "question with five answers, and would put semver on api/web, which deploy",
    "continuously and are never installed by anyone.",
    "",
    "So: a single root package. release-please owns version.txt and mirrors that",
    "version into the Android versionName and the iOS MARKETING_VERSION via the",
    "generic updater (the `x-release-please-version` markers in those files).",
    "",
    "Build NUMBERS are deliberately not here — versionCode and",
    "CURRENT_PROJECT_VERSION come from the git commit count at build time",
    "(monotonic by construction, see mobile.yml). This owns only the human-facing",
    "version.",
    "",
    "Releases start from the v1.0.0 baseline tag. Without it the first PR would",
    "have swept up all 506 commits of pre-launch history, whose body exceeds",
    "GitHub's 65,536-character PR limit — which is exactly how the first attempt",
    "failed."
  ],

  "release-type": "simple",

  "//tag-format": [
    "Tags are plain `v0.1.0`, NOT `loonext-v0.1.0`.",
    "",
    "This is load-bearing. A manifest config defaults include-component-in-tag to",
    "TRUE, so release-please looked for `loonext-v0.1.0`, found no previous",
    "release, and considered all 1045 commits of history — producing a PR body",
    "past GitHub's 65,536-character limit, which is why the first two runs failed",
    "at PR creation with an empty error. With one product version there is no",
    "component to disambiguate anyway."
  ],
  "include-component-in-tag": false,

  "packages": {
    ".": {
      "package-name": "loonext",
      "changelog-path": "CHANGELOG.md",
      "extra-files": [
        { "type": "generic", "path": "apps/android/app/build.gradle.kts" },
        { "type": "generic", "path": "apps/ios/project.yml" }
      ]
    }
  },

  "changelog-sections": [
    { "type": "feat", "section": "Features" },
    { "type": "fix", "section": "Bug Fixes" },
    { "type": "perf", "section": "Performance" },
    { "type": "revert", "section": "Reverts" },
    { "type": "refactor", "section": "Refactoring", "hidden": true },
    { "type": "docs", "section": "Documentation", "hidden": true },
    { "type": "test", "section": "Tests", "hidden": true },
    { "type": "ci", "section": "CI", "hidden": true },
    { "type": "build", "section": "Build", "hidden": true },
    { "type": "style", "section": "Style", "hidden": true },
    { "type": "chore", "section": "Chores", "hidden": true }
  ]
}
